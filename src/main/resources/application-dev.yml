spring:
  datasource:
    url: jdbc:mysql://localhost:3306/algafood?createDatabaseIfNotExist=true&serverTimeZone=UTC
    username: root
    password:
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true
